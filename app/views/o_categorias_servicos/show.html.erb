<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar O Categoria Servico</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @o_categoria_servico %>">
          <p>
            <strong>Descricao:</strong>
            <%= @o_categoria_servico.descricao %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_o_categoria_servico_path(@o_categoria_servico), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", o_categorias_servicos_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @o_categoria_servico, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
