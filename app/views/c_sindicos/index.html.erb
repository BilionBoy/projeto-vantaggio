<%= render "shared/context_nav", title: t("activerecord.models.#{CSindico.model_name.i18n_key}.other"), new_path: new_c_sindico_path do  %>
  <span class="breadcrumb-item active"><%= t("activerecord.models.#{CSindico.model_name.i18n_key}.other") %></span>
<% end %>
<div class="card">
  <div class="card-header d-flex">
    <%= search_form_for @q, html: { class: "d-flex gap-2" , autocomplete: "off"} do |f| %>
      <%= f.search_field :nome_cont, class: "form-control", placeholder: "Pesquisar" %>
      <%= f.button(type: 'submit', class: 'btn btn-info') { tag.i(class: 'ph-magnifying-glass') } %>
      <%= link_to  tag.i(class: 'ph-x-circle'), c_sindicos_path, class: "btn btn-light" if params[:q] %>
    <% end %>
  </div>
  <div class="card-body">
    <div class= "table-responsive">
      <table class="table table-sm table-bordered table-striped">
        <thead>
          <tr>
            <th class="text-center">Ações</th>
            <th><%= sort_link(@q, :nome, t("activerecord.attributes.c_sindico.nome")) %></th>
            <th><%= sort_link(@q, :telefone, t("activerecord.attributes.c_sindico.telefone")) %></th>
            <th><%= sort_link(@q, :cpf, t("activerecord.attributes.c_sindico.cpf")) %></th>
            <th><%= sort_link(@q, :c_condominio, t("activerecord.attributes.c_sindico.condominio")) %></th>
            <th><%= sort_link(@q, :g_tipo_usuario, t("activerecord.attributes.c_sindico.tipo_usuario")) %></th>
            <th><%= sort_link(@q, :g_status_usuario, t("activerecord.attributes.c_sindico.status_usuario")) %></th>
          </tr>
        </thead>
        <tbody>
          <% @c_sindicos.each do |c_sindico| %>
            <tr>
              <td class="text-center">
                <div class="dropdown-toggle">
                  <a href="#" class="text-body dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="ph-gear"></i>
                  </a>
                  <div class="dropdown-menu" style="">
                    <div class="dropdown-header">Opções</div>
                    <%= link_to edit_c_sindico_path(c_sindico), class: "dropdown-item" do %>
                      <i class="ph-pen me-2"></i> Editar
                    <% end %>
                    <%= form_for c_sindico, method: :delete do |f| %>
                      <a class="dropdown-item hand-pointer " data-confirm-delete="<%= c_sindico_path(c_sindico) %>" data-csrf-token="<%= form_authenticity_token %>">
                        <i class="ph-trash-simple me-2"></i>Excluir
                      </a>
                    <% end %>
                    <td><%= c_sindico.nome %></td>
                    <td><%= c_sindico.telefone %></td>
                    <td><%= c_sindico.cpf %></td>
                    <td><%= c_sindico.c_condominio&.nome %></td>
                    <td><%= c_sindico.g_tipo_usuario&.descricao %></td>
                    <td><%= c_sindico.g_status_usuario&.descricao %></td>
                  </div>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="card-footer">
    <%= pagy_bootstrap_nav(@pagy, link_extra: 'class="btn btn-outline-secondary btn-sm mx-1"').html_safe %>
  </div>
</div>
