<%= simple_form_for(@user) do |f| %>
  <div class="card shadow-sm border-0">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
      <h5 class="mb-0">
        <%= @user.new_record? ? "Novo Usuário" : "Editar Usuário" %>
      </h5>
    </div>
    <div class="card-body">
      <div class="row g-3">
        <div class="col-md-6">
          <%= f.input :nome, 
                      placeholder: "Digite o nome completo",
                      input_html: { class: "form-control" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :email, 
                      placeholder: "exemplo@empresa.com",
                      input_html: { class: "form-control" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :telefone, 
                      placeholder: "(99) 99999-9999",
                      input_html: { class: "form-control telefone" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :a_tipo_usuario_id, 
                      collection: ATipoUsuario.all,
                      label_method: :descricao,
                      value_method: :id,
                      prompt: "Selecione o tipo de usuário",
                      input_html: { class: "form-select" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :password,
                      placeholder: "Digite uma senha (opcional se for auto-gerada)",
                      input_html: { autocomplete: "new-password", class: "form-control" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :password_confirmation,
                      placeholder: "Confirme a senha",
                      input_html: { autocomplete: "new-password", class: "form-control" } %>
        </div>
      </div>
    </div>
    <div class="card-footer d-flex justify-content-between bg-light">
      <%= link_to "↩️ Voltar", users_path, class: "btn btn-outline-secondary" %>
      <%= f.button :submit, "💾 Salvar", class: "btn btn-success" %>
    </div>
  </div>
<% end %>
