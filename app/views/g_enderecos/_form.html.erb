
<%= render "shared/context_nav", title: t("activerecord.models.#{GEndereco.model_name.i18n_key}.other") do  %>
<span class="breadcrumb-item"><%= link_to t("activerecord.models.#{GEndereco.model_name.i18n_key}.other"), g_enderecos_path, class: "text-dark" %></span>
<span class="breadcrumb-item active"><%= t("helpers.submit.#{params[:action]}") %></span>
<% end %>
<%= simple_form_for(@g_endereco) do |f| %>
<div class="card">
  <div class="card-header">
    <h5 class="my-0">
      <%= @g_endereco.new_record? ? "Incluir " : "Editar " %><%= t("activerecord.models.#{GEndereco.model_name.i18n_key}.one") %>
    </h5>
  </div>
  <div class="card-body">
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
    <div class="row">
      
        
          <div class="col-12">
            <%= f.input :cep, input_html: { class: "capitalize"}, placeholder: "Cep"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.input :rua, input_html: { class: "capitalize"}, placeholder: "Rua"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.input :bairro, input_html: { class: "capitalize"}, placeholder: "Bairro"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.input :numero, input_html: { class: "capitalize"}, placeholder: "Numero"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.association :g_municipio, input_html: { class: "capitalize"}, placeholder: "G municipio"  %>
          </div>
        
    </div>
  </div>
  <div class="card-footer">
    <%= btn_submit(f) %>
    <%= link_to "Voltar", g_enderecos_path, class: "btn btn-light" %>
  </div>
</div>
<% end %>